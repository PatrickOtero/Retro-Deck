{
  "compilerOptions": {
    "target": "ES2020",                        // O código será compilado para ES2020
    "module": "CommonJS",                      // Usamos CommonJS para compatibilidade com Node
    "strict": true,                            // Ativa todas as verificações de tipo do TypeScript
    "esModuleInterop": true,                   // Habilita a importação de módulos CommonJS como ES6
    "skipLibCheck": true,                      // Ignora a verificação de tipos em bibliotecas externas
    "forceConsistentCasingInFileNames": true,  // Garante que os nomes de arquivos sejam consistentes em termos de maiúsculas/minúsculas
    "outDir": "./dist",                        // Pasta de saída para os arquivos JavaScript compilados
    "baseUrl": ".",                            // Base para caminhos relativos
    "paths": {
      "*": ["node_modules/*"]                  // Define os caminhos para resolver pacotes
    },
  },
  "include": [
    "src/**/*"                                 // Incluir todos os arquivos na pasta src
  ],
  "exclude": [
    "node_modules"                             // Excluir a pasta node_modules da compilação
  ]
}
